@using KCC.Web.Features.Sections.BentoBox

@model BentoBoxSectionViewModel

@{
    // var isContainer = Model.ContentWidth == "Container";
    // @(isContainer ? "container mx-auto" : "w-full")
}

<div data-section-guid="@Model.SectionGuid" class="@Html.Clsx(Model.BackgroundColor, Model.TextColor)">
    <div class="variable-section-wrapper">
        <div class="variable-section pb-@Model.PaddingBottom pt-@Model.PaddingTop">
            <div>
                <widget-zone />
            </div>
        </div>
    </div>
</div>
